'use client';import {useEffect,useState} from 'react';import {BarChart3,Users,Briefcase,CheckCircle2} from 'lucide-react';
export default function Admin(){const [apps,setApps]=useState<any[]>([]);const [jobs,setJobs]=useState<any[]>([]);useEffect(()=>{fetch('/api/applications').then(r=>r.json()).then(setApps);fetch('/api/jobs').then(r=>r.json()).then(setJobs)},[]);const counts=['APPLIED','SCREENING','SHORTLISTED','INTERVIEW','OFFERED','HIRED'].map(s=>[s,apps.filter(a=>a.status===s).length]);return <main className="mx-auto max-w-7xl px-5 py-10"><p className="text-sm font-bold text-green-300">ADMIN ANALYTICS</p><h1 className="mt-2 text-4xl font-black">Hiring intelligence.</h1><div className="mt-8 grid gap-4 md:grid-cols-4">{[[Briefcase,'Live jobs',jobs.length],[Users,'Applications',apps.length],[CheckCircle2,'Hired',apps.filter(a=>a.status==='HIRED').length],[BarChart3,'Avg. match',apps.length?Math.round(apps.reduce((a,b)=>a+b.matchScore,0)/apps.length)+'%':'—']].map(([I,t,n]:any)=><div className="glass rounded-2xl p-5" key={t}><I className="text-green-300"/><p className="mt-4 text-3xl font-black">{n}</p><p className="text-sm text-zinc-500">{t}</p></div>)}</div><section className="glass mt-6 rounded-3xl p-6"><h2 className="text-xl font-bold">Recruitment funnel</h2><div className="mt-7 grid gap-3 md:grid-cols-6">{counts.map(([s,n]:any)=><div key={s} className="rounded-2xl bg-white/[.03] p-4"><p className="text-xs text-zinc-500">{s}</p><p className="mt-3 text-2xl font-black">{n}</p><div className="mt-3 h-1 rounded-full bg-green-400" style={{opacity:n?1:.2}}/></div>)}</div></section></main>}
