'use client';import {useEffect,useState} from 'react';import {Users,BriefcaseBusiness,Plus,ArrowRight,X,MapPin,IndianRupee,CheckCircle2} from 'lucide-react';import Link from 'next/link';
const empty={title:'',company:'',location:'Ahmedabad, India',workMode:'HYBRID',employmentType:'FULL_TIME',salaryMin:'',salaryMax:'',description:'',requirements:'',benefits:'',skills:''};
export default function Recruiter(){const [jobs,setJobs]=useState<any[]>([]);const [apps,setApps]=useState<any[]>([]);const [show,setShow]=useState(false);const [form,setForm]=useState(empty);const [saving,setSaving]=useState(false);const [message,setMessage]=useState('');const load=()=>{fetch('/api/recruiter/jobs').then(r=>r.json()).then(setJobs);fetch('/api/applications').then(r=>r.json()).then(setApps)};useEffect(load,[]);const submit=async(e:any)=>{e.preventDefault();setSaving(true);setMessage('');const payload={...form,salaryMin:form.salaryMin?Number(form.salaryMin):null,salaryMax:form.salaryMax?Number(form.salaryMax):null,requirements:form.requirements.split(',').map(x=>x.trim()).filter(Boolean),benefits:form.benefits.split(',').map(x=>x.trim()).filter(Boolean),skills:form.skills.split(',').map(x=>x.trim()).filter(Boolean)};const r=await fetch('/api/recruiter/jobs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(r.ok){setForm(empty);setShow(false);setMessage('Job published successfully.');load()}else setMessage('Could not publish this job.');setSaving(false)};return <main className="mx-auto max-w-7xl px-5 py-10 md:py-14"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><div className="pill inline-flex px-3 py-1.5 text-xs font-bold text-green-300">RECRUITER WORKSPACE</div><h1 className="mt-4 text-4xl font-black tracking-tight md:text-5xl">Your hiring <span className="gradient-text">command center.</span></h1><p className="mt-3 max-w-2xl text-zinc-500">Publish roles, review candidates and move the right people through your pipeline.</p></div><button onClick={()=>setShow(true)} className="rounded-2xl bg-green-400 px-5 py-3.5 font-black text-black shadow-[0_12px_40px_rgba(74,222,128,.12)]"><Plus className="mr-2 inline" size={17}/>Add New Job</button></div>{message&&<div className="mt-5 rounded-2xl border border-green-400/20 bg-green-400/5 p-4 text-sm text-green-300"><CheckCircle2 className="mr-2 inline" size={16}/>{message}</div>}<div className="mt-8 grid gap-4 md:grid-cols-4">{[[BriefcaseBusiness,'Open jobs',jobs.length],[Users,'Applicants',apps.length],[Users,'Shortlisted',apps.filter(a=>a.status==='SHORTLISTED').length],[ArrowRight,'Interviews',apps.filter(a=>a.status==='INTERVIEW').length]].map(([I,t,n]:any)=><div className="glass rounded-2xl p-5" key={t}><I className="text-green-300"/><p className="mt-4 text-2xl font-black">{n}</p><p className="text-sm text-zinc-500">{t}</p></div>)}</div><div className="mt-8 grid gap-6 lg:grid-cols-[1.05fr_.95fr]"><section className="glass rounded-3xl p-6"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">Active roles</h2><p className="mt-1 text-sm text-zinc-500">Your published opportunities.</p></div><button onClick={()=>setShow(true)} className="rounded-xl border border-white/10 px-3 py-2 text-xs font-bold"><Plus className="mr-1 inline" size={13}/>New role</button></div><div className="mt-5 space-y-3">{jobs.map(j=><div className="rounded-2xl border border-white/[.07] bg-white/[.02] p-4" key={j.id}><div className="flex items-start justify-between gap-4"><div><p className="font-bold">{j.title}</p><p className="mt-1 text-xs text-zinc-500"><MapPin size={12} className="mr-1 inline"/>{j.location} · {j._count.applications} applicants</p></div><span className="rounded-full bg-green-400/10 px-3 py-1 text-xs font-bold text-green-300">OPEN</span></div><div className="mt-3 flex gap-2 text-xs text-zinc-500"><span className="pill px-2.5 py-1">{j.workMode}</span><span className="pill px-2.5 py-1">{j.employmentType.replace('_',' ')}</span></div></div>)}{jobs.length===0&&<div className="py-10 text-center text-sm text-zinc-500">No roles yet. Click <b className="text-white">Add New Job</b> to publish your first role.</div>}</div></section><section className="glass rounded-3xl p-6"><div><h2 className="text-xl font-bold">Candidate pipeline</h2><p className="mt-1 text-sm text-zinc-500">Latest applications and match signals.</p></div><div className="mt-5 space-y-3">{apps.slice(0,8).map(a=><div className="rounded-2xl border border-white/[.07] p-4" key={a.id}><div className="flex items-center justify-between gap-3"><div><p className="font-bold">{a.candidate.name}</p><p className="mt-1 text-xs text-zinc-500">{a.job.title}</p></div><span className="rounded-full bg-green-400/10 px-2.5 py-1 text-xs font-bold text-green-300">{a.matchScore}% match</span></div><div className="mt-3 flex items-center justify-between text-xs"><span className="text-zinc-500">{a.status}</span><span className="text-zinc-600">Application #{a.id}</span></div></div>)}{apps.length===0&&<div className="py-10 text-center text-sm text-zinc-500">Applicants will appear here after candidates apply.</div>}</div></section></div><div className="mt-6 text-center"><Link href="/admin" className="text-sm font-bold text-green-300">Open hiring analytics →</Link></div>{show&&<div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/75 p-4 backdrop-blur-md"><div className="max-h-[92vh] w-full max-w-3xl overflow-y-auto rounded-[2rem] border border-white/10 bg-[#0a100c] shadow-2xl"><div className="sticky top-0 z-10 flex items-center justify-between border-b border-white/10 bg-[#0a100c]/95 px-6 py-5 backdrop-blur-xl"><div><p className="text-xs font-bold uppercase tracking-wider text-green-300">Employer tools</p><h2 className="mt-1 text-2xl font-black">Create a new job</h2></div><button onClick={()=>setShow(false)} className="rounded-xl border border-white/10 p-2"><X size={18}/></button></div><form onSubmit={submit} className="grid gap-5 p-6"><div className="grid gap-4 md:grid-cols-2"><label className="text-sm">Job title<input required className="field mt-2" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} placeholder="e.g. Software Engineer"/></label><label className="text-sm">Company<input required className="field mt-2" value={form.company} onChange={e=>setForm({...form,company:e.target.value})} placeholder="e.g. Acme Technologies"/></label><label className="text-sm">Location<input required className="field mt-2" value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/></label><label className="text-sm">Work mode<select className="field mt-2" value={form.workMode} onChange={e=>setForm({...form,workMode:e.target.value})}><option>REMOTE</option><option>HYBRID</option><option>ONSITE</option></select></label><label className="text-sm">Employment type<select className="field mt-2" value={form.employmentType} onChange={e=>setForm({...form,employmentType:e.target.value})}><option>FULL_TIME</option><option>PART_TIME</option><option>CONTRACT</option><option>INTERNSHIP</option></select></label><div className="grid grid-cols-2 gap-3"><label className="text-sm">Min salary<div className="relative mt-2"><IndianRupee className="absolute left-3 top-3 text-zinc-500" size={15}/><input type="number" className="field pl-9" value={form.salaryMin} onChange={e=>setForm({...form,salaryMin:e.target.value})} placeholder="600000"/></div></label><label className="text-sm">Max salary<input type="number" className="field mt-2" value={form.salaryMax} onChange={e=>setForm({...form,salaryMax:e.target.value})} placeholder="1200000"/></label></div></div><label className="text-sm">Role description<textarea required rows={4} className="field mt-2" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} placeholder="Describe the role, team and impact..."/></label><label className="text-sm">Skills <span className="text-zinc-600">(comma separated)</span><input required className="field mt-2" value={form.skills} onChange={e=>setForm({...form,skills:e.target.value})} placeholder="React, TypeScript, Node.js, PostgreSQL"/></label><div className="grid gap-4 md:grid-cols-2"><label className="text-sm">Requirements <span className="text-zinc-600">(comma separated)</span><textarea rows={3} className="field mt-2" value={form.requirements} onChange={e=>setForm({...form,requirements:e.target.value})} placeholder="2+ years experience, REST APIs, Git"/></label><label className="text-sm">Benefits <span className="text-zinc-600">(comma separated)</span><textarea rows={3} className="field mt-2" value={form.benefits} onChange={e=>setForm({...form,benefits:e.target.value})} placeholder="Remote work, insurance, learning budget"/></label></div><div className="flex flex-col-reverse justify-end gap-2 border-t border-white/10 pt-5 sm:flex-row"><button type="button" onClick={()=>setShow(false)} className="rounded-xl border border-white/10 px-5 py-3 text-sm font-bold">Cancel</button><button disabled={saving} className="rounded-xl bg-green-400 px-6 py-3 text-sm font-black text-black disabled:opacity-50">{saving?'Publishing...':'Publish Job'}</button></div></form></div></div>}</main>}
