'use client';
import Link from 'next/link';import {Bookmark,MapPin,Clock3,Sparkles,ArrowUpRight,Building2} from 'lucide-react';import {money} from '../lib/utils';
export default function JobCard({job}:{job:any}){return <article className="glass group rounded-[1.7rem] p-5 transition duration-300 hover:-translate-y-1 hover:border-green-400/25 hover:shadow-[0_20px_70px_rgba(0,0,0,.3)]"><div className="flex items-start justify-between gap-4"><div className="flex min-w-0 gap-3"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl border border-green-400/10 bg-green-400/[.08] font-black text-green-300">{job.company.slice(0,2).toUpperCase()}</div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="truncate font-bold">{job.title}</h3>{job.featured&&<span className="rounded-full bg-green-400/10 px-2 py-1 text-[10px] font-bold text-green-300"><Sparkles size={10} className="mr-1 inline"/>Featured</span>}</div><p className="mt-1 text-sm text-zinc-400"><Building2 size={13} className="mr-1 inline"/>{job.company}</p></div></div><button className="rounded-xl p-2 text-zinc-500 hover:bg-white/5 hover:text-white"><Bookmark size={18}/></button></div><div className="mt-5 flex flex-wrap gap-2 text-xs"><span className="pill px-3 py-1.5 text-zinc-300"><MapPin size={12} className="mr-1 inline"/>{job.location}</span><span className="pill px-3 py-1.5 text-zinc-300">{job.workMode}</span><span className="pill px-3 py-1.5 text-zinc-300"><Clock3 size={12} className="mr-1 inline"/>{job.employmentType.replace('_',' ')}</span></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-zinc-400">{job.description}</p><div className="mt-4 flex flex-wrap gap-2">{job.skills.slice(0,4).map((s:string)=><span key={s} className="rounded-lg bg-white/[.035] px-2.5 py-1.5 text-xs text-zinc-300">{s}</span>)}</div><div className="mt-5 flex items-center justify-between border-t border-white/[.07] pt-4"><span className="font-bold text-green-300">{money(job.salaryMin)} – {money(job.salaryMax)}</span><Link href={`/jobs/${job.id}`} className="rounded-xl bg-white px-4 py-2.5 text-sm font-bold text-black transition hover:bg-green-400">View role <ArrowUpRight size={15} className="ml-1 inline"/></Link></div></article>}
